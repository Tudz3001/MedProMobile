<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Quyền thông báo (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Quyền đặt báo thức chính xác — CỰC QUAN TRỌNG cho nhắc thuốc đúng giờ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- Cho phép hoạt động sau khi reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Vibration -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Wake lock để đánh thức màn hình khi có thông báo -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Internet (không bắt buộc, nhưng cần nếu dùng TTS online) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Receiver để khởi động lại alarm sau khi reboot máy -->
        <receiver
            android:name="io.notifee.core.NotifeeAlarmManager"
            android:exported="false"/>

        <receiver
            android:name="io.notifee.core.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Notifee headless task (xử lý notification khi app đóng) -->
        <service
            android:name="io.notifee.core.HeadlessTaskService"
            android:exported="false"/>

    </application>
</manifest>
